# LabSystem (React scaffold)

Folders created: `src/components`, `src/screens`, `src/api`, `src/hooks`, `src/services`, `src/utils`, `src/assets`.

Quick start (Windows):

```bash
npm install
npm run dev
```
