import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#0366d6'
    },
    background: {
      default: '#f7f7fb'
    }
  },
})

export default theme
